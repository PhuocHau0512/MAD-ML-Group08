streamlit         # Thư viện chính để xây dựng giao diện web
pandas            # Dùng để đọc và xử lý dữ liệu (CSV)
numpy             # Dùng cho các phép toán số học và xử lý mảng (ví dụ: np.where)
scikit-learn==1.3.2 # Thư viện Machine Learning chính (chứa SVM, Naive Bayes, Decision Forest)
joblib            # Dùng để lưu và tải các tệp mô hình (.joblib)
pefile            # Dùng để đọc và trích xuất đặc trưng từ tệp .exe (PE)
androguard        # Dùng để phân tích tệp .apk (hướng phát triển tương lai)
matplotlib        # Dùng để vẽ các biểu đồ (biểu đồ tròn, biểu đồ cột)